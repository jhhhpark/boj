#include <iostream>

using namespace std;

int problem_2742()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	int N = 0;
	cin >> N;
	for (int i = N; i > 0; --i)
	{
		cout << i << "\n";
	}

	return 0;
}